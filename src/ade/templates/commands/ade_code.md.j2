Run ADE coding phases (Design Check + Implement + Quality Gate) for: $ARGUMENTS

If a plan exists in .ade/tasks/, use it. Otherwise, run /ade-plan first.
1. Phase 3 — DESIGN CHECK: Generate stubs with CrewAI architect agent (max 2x)
2. Phase 4 — IMPLEMENT: Dispatch CrewAI coder in a git worktree
   Enforce build order: shared types → backend → frontend
3. Phase 5 — QUALITY GATE: pre-commit + tests + test gap analysis

Report the QA gate result when complete.
