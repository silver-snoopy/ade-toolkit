Run the ADE GitHub PR review-and-fix loop for: $ARGUMENTS

$ARGUMENTS may be a PR number, a PR URL, or empty (auto-detect the PR for the
current branch via `gh pr view --json number`).

Follow the ade-pr-review skill defined under `.claude/skills/ade/`.
