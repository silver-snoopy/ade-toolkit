# {{ info.project_name }}

One or two sentences describing what this project is and why it exists. Replace this with your own description when you have something concrete to say — `grill-with-docs` will help shape it during your first spec.

## Language

This section is a glossary of domain terms. ADE's Research phase populates it incrementally as `grill-with-docs` sharpens terminology during spec creation. Do not pre-fill it speculatively — terms accumulate naturally as decisions crystallise.

Entries follow this format:

```
**Term**:
One or two sentences defining what the term IS (not what it does).
_Avoid_: Alternative words to NOT use for this concept.
```

Only include terms specific to this project's domain. General programming concepts (timeout, retry, cache, queue) do not belong here even if the project uses them.

<!--
The first entry will be added when grill-with-docs resolves the first ambiguous term during a Research phase. See:
- src/ade/templates/skills/grill-with-docs/CONTEXT-FORMAT.md (full format)
- src/ade/templates/skills/grill-with-docs/SKILL.md (how it updates this file)
-->
